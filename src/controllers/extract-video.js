/*
* Extracts region from base64 image using coordinates
* compressionLevel = 1 to 10
*/
async function extract(video, extractOptions, closeWhenFinished) {
  console.log('Started extracting image');
  console.dir(video);
}

module.exports = extract;
